[project]
name = "despliegueautomatizacion"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "FerMelero", email = "fmeleromunoz@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "numpy>=2.5.3",
    "pandas>=3.0.5",
]

[project.scripts]
despliegueautomatizacion = "despliegueautomatizacion:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
